# Ralph Progress Log
Started: Mon Mar 17 2026

## Codebase Patterns
- Unity build: all src/*.c files included via src/jacl.c
- Test pattern: test_helpers.h, tracker_reset(), check_no_leaks(), PrintCapture for stdout
- 64-bit tagged values: tag in bits 63-56, payload in bits 55-0
- Existing string tags: JACL_TAG_INLINE_STRING (0x04), JACL_TAG_STRING (0x05)
- JaclHeapString: { byte_len, grapheme_len, hash, data[] } allocated via gc_alloc(heap, OBJ_STRING, size)
- Intern table: open addressing, linear probing, arena-backed
- jacl_string_len() returns grapheme count; jacl_string_byte_len() returns byte count
- unicode.h included in unity build (src/jacl.c) before string.c
- Rope lib: lib/sum_tree/rope.h (B-tree with monoidal summaries, RC-based persistence)
- Unicode lib: lib/unicode/unicode.h (NFD normalization, grapheme breaks UAX #29, UTF-8 codec)
- Rope summary: { size_t bytes, chars, lines, graphemes }
- Compiler flags: -Wall -Wextra -std=c99 -g
- rope_concat: grapheme-safe concat in rope.h, fixes combining chars at junction
- Rope leaf access: walk right spine for rightmost leaf, left spine for leftmost
- rope_st_split at exact leaf boundary: split at (total_bytes - leaf_count) gives empty left + full leaf right
- 6 pre-existing compile failures: segment_array, sum_tree, utf8, rope, nfa, nfa_rope (include path issues)

---

## 2026-03-17 - US-002
Thread: N/A (autonomous agent)
- Implemented `rope_concat()` in lib/sum_tree/rope.h with grapheme boundary invariant
- After concat, checks if right starts with Extend/ZWJ/SpacingMark codepoints
- If combining chars found: splits both ropes, builds junction buffer (left's last leaf + combining prefix), creates new rope from junction via rope_from_str (which handles grapheme-safe leaf splitting), concatenates prefix + junction + rest
- O(1) fast path when right doesn't start with combining chars (just delegates to rope_st_concat)
- Files changed: lib/sum_tree/rope.h (added rope_concat), lib/sum_tree/test_rope_concat.c (new, 8 tests), build.sh (added test entry)
- **Learnings for future iterations:**
  - rope_summary_combine is purely additive for graphemes — it overcounts when combining chars span the junction
  - The fix-up must ensure combining bytes end up in the same leaf as their base character for correct grapheme counting
  - rope_from_str handles grapheme_find_safe_split internally, so building a junction buffer and calling rope_from_str correctly handles leaf overflow
  - Reference counting: rope_st_split creates owned roots that must be unreffed; rope_st_concat returns a new owned root
---

## 2026-03-17 - US-003
Thread: N/A (autonomous agent)
- Updated JaclHeapString struct: renamed `length` → `byte_len`, added `grapheme_len` field
- `grapheme_len` computed at creation time in `jacl_intern()` via `unicode_grapheme_count()`
- `jacl_string_len()` now returns grapheme count (inline: computed on-the-fly, heap: cached field)
- Added `jacl_string_byte_len()` for byte length across both tiers
- All callers that need byte length updated to `jacl_string_byte_len()` (vm.c concat/print/format, string.c eq/cmp, embed.c)
- OP_STR_LEN still uses `jacl_string_len()` — now returns grapheme count to user code
- Added `unicode.h` include to `src/jacl.c` unity build
- Files changed: src/jacl.c, src/string.c, src/vm.c, src/embed.c, test/test_string.c
- 5 new tests: combining marks grapheme_len, ASCII grapheme_len, byte_len inline/heap, grapheme inline
- **Learnings for future iterations:**
  - When changing jacl_string_len() semantics, ALL callers must be audited for byte vs grapheme intent
  - OP_STR_INDEX and OP_STR_SLICE still use byte-level operations — US-008/US-009 will convert to grapheme-based
  - The struct layout change (adding grapheme_len) affects sizeof(JaclHeapString) — gc_alloc uses sizeof + byte_len
  - For inline strings, grapheme count is computed on-the-fly (max 7 bytes, trivially fast)
---
