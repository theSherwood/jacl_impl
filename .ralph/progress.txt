# Ralph Progress Log
Started: Sun Apr 20 2026
Feature: Shell Interop (!cmd)

## Codebase Patterns
- Lexer already has many token types defined - check existing TOKEN_* before adding new ones
- Lexer tests are in test/test_lexer.c with a main() test runner at the end
- Tests follow pattern: setup(), lexer_lex(), assertions, teardown(), check_no_leaks()
- `-flag` syntax lexes as TOKEN_OPERATOR (`-`) + TOKEN_WORD (`flag`), not a single token
- build.sh runs all tests and reports pass/fail counts
- AST node types defined in both src/ast.c AND src/jacl.h - update both when adding new types
- Parser tests in test/test_parser.c use parse() helper and ASSERT macros
- parser__is_operand_end() stops at operators - shell commands need custom check
- Compiler has ~12 switch statements for AST node traversal - add new node types to ALL of them
- To create empty HAMT map: jacl_map_ptr(NULL), NOT jacl_map_empty()
- Prelude maps use c.has_prelude flag and compiler__find_global() for name checking

---

## 2026-04-20 - US-001
Thread: (current)
- Verified TOKEN_BANG already exists in lexer (lexer.c:46)
- Verified `!` at command position produces TOKEN_BANG (lines 1462-1469)
- Added 8 comprehensive lexer tests for shell interop syntax:
  - test_bang_ls: `!ls` → BANG + WORD
  - test_bang_git_status: `!git status` → BANG + WORD + WORD
  - test_bang_ls_la: `!ls -la` → BANG + WORD + OPERATOR + WORD
  - test_bang_with_var: `!$cmd` → BANG + VAR
  - test_bang_with_string: `!"my script.sh"` → BANG + STRING
  - test_bang_with_splat: `!cmd ..$args` → BANG + WORD + DOTDOT + VAR
  - test_bang_in_pipeline: `!ls | !grep foo` → proper pipeline tokens
  - test_bang_neq_distinction: `!=` stays TOKEN_OPERATOR
- Files changed: test/test_lexer.c
- **Learnings for future iterations:**
  - TOKEN_BANG was already implemented for other purposes - lexer was ahead of the PRD
  - `-` followed by letters is not a single operator; it splits into OPERATOR + WORD
  - Test helper `token_text_eq` compares token length with text length
  - Test helper `var_text_eq` skips the `$` prefix (token.length - 1)
---

## 2026-04-20 - US-002
Thread: (current)
- Added AST_SHELL_CMD node type to ast.c and jacl.h
- Added shell_cmd data struct with head, args, arg_count fields
- Added parser__parse_shell_cmd logic in parser__parse_cmd_operand
- Created parser__is_shell_cmd_end() to stop at pipe/and/or/amp but not at - operators
- Added pretty-print support for AST_SHELL_CMD (outputs !cmd arg1 arg2)
- Added 10 parser tests:
  - test_shell_cmd_simple: `!ls`
  - test_shell_cmd_with_arg: `!git status`
  - test_shell_cmd_multiple_args: `!git status --short`
  - test_shell_cmd_flag_args: `!ls -la`
  - test_shell_cmd_quoted_name: `!"my script.sh"`
  - test_shell_cmd_var_name: `!$cmd`
  - test_shell_cmd_splat_args: `!cmd ..$args`
  - test_shell_cmd_in_pipeline: `!ls | !grep foo`
  - test_shell_cmd_error_no_name: `!` alone is error
  - test_shell_cmd_neq_not_shell: `1 != 2` stays as operator
- Files changed: src/ast.c, src/jacl.h, src/parser.c, test/test_parser.c
- **Learnings for future iterations:**
  - AST node types must be added to both ast.c and jacl.h (duplicate definitions)
  - Shell commands should not stop at TOKEN_OPERATOR (e.g., `-`) since flags like `-la` are valid
  - Shell commands should stop at TOKEN_PIPE, TOKEN_AND, TOKEN_OR, TOKEN_AMP for proper pipeline support
  - parser__parse_expr handles atoms and can parse TOKEN_OPERATOR as string literals in atom position
---

## 2026-04-20 - US-003
Thread: (current)
- Added AST_SHELL_CMD case to compiler__compile_node:
  - Checks exec availability in prelude mode (compile error if not available)
  - Transforms !cmd args... into [exec head arg1 arg2...] call
  - Handles spread (..$args) via OP_CALL_SPREAD path
  - Emits OP_GET_GLOBAL for exec, compiles head/args, then OP_CALL
- Added AST_SHELL_CMD handling to ALL 12 analysis/traversal functions:
  - analyze__walk_body (records exec as callee)
  - analyze__collect_procs (recurses into head/args)
  - sm__walk_suspensions (recurses)
  - sm__walk_locals (recurses)
  - sm__liveness_walk (recurses)
  - ast__contains_suspension (recurses)
  - ast__collect_local_muts (recurses)
  - ast__contains_nonlocal_set_impl (recurses)
  - ast__collect_local_names (recurses)
  - ast__refs_nonlocal_mutable_impl (recurses)
  - syntax__compile_sq_node already has default fallback
- Added 6 compiler tests:
  - test_shell_cmd_compile_simple: basic !ls -la compiles
  - test_shell_cmd_compile_vars: !$cmd $arg1 with variable refs
  - test_shell_cmd_compile_splat: !cmd ..$args with spread
  - test_shell_cmd_no_exec_error: empty prelude produces "exec not available"
  - test_shell_cmd_with_exec_prelude: prelude with exec compiles successfully
  - test_shell_cmd_quoted_name: !"my script.sh" compiles
- Files changed: src/compiler.c, test/test_compiler.c
- **Learnings for future iterations:**
  - Compiler has many switch statements on node->type; must add cases to ALL of them
  - For prelude tests: use jacl_map_ptr(NULL) for empty map, jacl_map_set(NULL, k, v) to add entries
  - Shell commands transform to calls to exec (prelude function), not a new opcode
---

## 2026-04-20 - US-004
Thread: (current)
- Added OP_EXEC opcode to bytecode (src/bytecode.c)
- Added STREAM_KIND_EXEC constant (value 5) for exec streams (src/gc.c, src/jacl.h)
- Added "exec" to jacl_non_core_builtins list for sandbox capability control
- Updated compiler to emit OP_VEC + OP_EXEC directly for AST_SHELL_CMD:
  - Compiles head and args, emits OP_VEC to create args vector, then OP_EXEC
  - Added compile-time error for spread in shell commands (OP_VEC doesn't support it)
- Implemented OP_EXEC in VM:
  - Pops args vector, builds shell-quoted command string
  - Calls popen() to spawn process
  - Creates STREAM_KIND_EXEC stream with FILE* stored in args[0]
- Implemented STREAM_KIND_EXEC handling in vm__pull_stream_one:
  - Reads stdout line-by-line via fgets for lazy evaluation
  - On EOF, pclose() and set STREAM_EXHAUSTED
- Special-cased OP_COLLECT for exec streams:
  - Reads all remaining output via fread
  - Returns concatenated result as single string
- Added AST_SHELL_CMD case to syntax_from_ast (src/syntax.c)
- Added 5 integration tests:
  - test_shell_exec_echo_basic: basic !echo hello
  - test_shell_exec_multiple_args: !echo a b c
  - test_shell_exec_direct_call: {!echo hello | collect}
  - test_shell_exec_stream_lazy: large output doesn't buffer fully
  - test_shell_exec_inherits_env: child inherits parent env
- Updated test_bytecode.c: OP_HALT now 156, OP_EXEC = 155
- Files changed: src/bytecode.c, src/compiler.c, src/gc.c, src/jacl.h, src/syntax.c, src/vm.c, test/test_bytecode.c, test/test_compiler.c, test/test_integration.c
- **Learnings for future iterations:**
  - popen() is simpler than fork/exec for basic stdout capture
  - Storing FILE* in stream args[0] requires uintptr_t cast
  - Lexer splits paths like `/tmp` into `/` and `tmp` tokens - tests must avoid paths with special chars
  - OP_VEC expects fixed element count, doesn't support spread - spread in shell commands deferred
---

## 2026-04-20 - US-005
Thread: (current)
- Implemented error handling for shell commands based on exit codes:
  - Exit code 0: returns stdout stream (unchanged from US-004)
  - Exit code non-zero: returns JACL error value with stderr content
- Modified OP_EXEC to redirect stderr to temp file (/tmp/jacl_stderr_PID_HASH)
- Modified vm__pull_stream_one STREAM_KIND_EXEC section:
  - On EOF, pclose() and check exit status via WEXITSTATUS()
  - If exit code != 0, read stderr temp file and create error value
  - Error message is stderr content, or "command exited with code N" if empty
- Modified OP_COLLECT STREAM_KIND_EXEC section with same error handling
- Added error propagation for stream for loops:
  - After OP_STREAM_NEXT, emit OP_JUMP_IF_ERROR to detect errors
  - Error exit path uses OP_CLOSE_LOOP to preserve error value while popping locals
- Added 6 integration tests:
  - test_shell_exec_error_false: !false returns error (exit 1)
  - test_shell_exec_error_stderr: stderr content captured in error message
  - test_shell_exec_success_no_error: exit 0 returns non-error value
  - test_shell_exec_error_pipe_compose: error propagates through for loop
  - test_shell_exec_error_try_catch: error works with try/catch
  - test_shell_exec_success_returns_stdout: success returns stdout content
- Files changed: src/compiler.c, src/vm.c, test/test_integration.c
- **Learnings for future iterations:**
  - OP_CLOSE_LOOP pops N values under TOS while preserving TOS - use for cleanup with result
  - OP_POP_N pops from top - wrong when result value needs to be preserved
  - In JACL, barewords in blocks are variable/command lookups, not string literals - use quotes
  - try/catch handler blocks require proper string literals or defined procs, not undefined barewords
  - pclose() returns exit status that must be decoded with WEXITSTATUS() macro
  - Temp files for stderr need unique names (PID + hash) to avoid collisions
---

## 2026-04-20 - US-006
Thread: (current)
- Added OP_EXEC_FULL opcode to bytecode.c, jacl.h
- Modified [exec ...] builtin in compiler.c to use OP_EXEC_FULL instead of OP_EXEC
- !cmd syntax (AST_SHELL_CMD) still uses OP_EXEC for stream-with-error semantics
- Added STREAM_KIND_EXEC_BUFFER constant (value 6) for pre-collected exec output
- Implemented OP_EXEC_FULL in vm.c:
  - Builds command string with stderr redirect to temp file (same as OP_EXEC)
  - Uses popen() to spawn process and reads ALL stdout into buffer
  - Reads stderr from temp file after process completes
  - Gets exit code via pclose() + WEXITSTATUS()
  - Creates STREAM_KIND_EXEC_BUFFER stream from stdout content
  - Creates map with {stdout: stream, stderr: string, exit: i32}
- Implemented STREAM_KIND_EXEC_BUFFER handling in vm__pull_stream_one:
  - Yields lines WITH trailing newlines (like STREAM_KIND_EXEC)
  - Advances position index after each line
- Implemented STREAM_KIND_EXEC_BUFFER handling in OP_COLLECT:
  - Returns the full pre-collected stdout string (not a vector of lines)
- Updated test_shell_exec_direct_call to use new [exec ...] semantics
- Added 6 integration tests:
  - test_exec_full_returns_map: basic map structure with exit field
  - test_exec_full_stdout_stream: stdout is a stream that can be collected
  - test_exec_full_stderr: stderr captures error output
  - test_exec_full_nonzero_exit: works with exit code 1 (no error value)
  - test_exec_full_both_outputs: captures both stdout and stderr
  - test_exec_full_stdout_lines: stdout can iterate line by line
- Files changed: src/bytecode.c, src/compiler.c, src/gc.c, src/jacl.h, src/vm.c, test/test_bytecode.c, test/test_integration.c
- **Learnings for future iterations:**
  - Maps support -> field access via OP_STRUCT_GET_DYN - no need for registered struct types
  - STREAM_KIND_EXEC collects to single string; derived streams (LINES, etc.) collect to vector
  - Created STREAM_KIND_EXEC_BUFFER to combine: line-by-line iteration + string collection
  - jacl_map_ptr() converts jacl_map_node* to JaclVal, not jacl_map_val()
  - jacl_map_set(NULL, k, v) creates new map from empty; can chain set calls
---

## 2026-04-21 - US-007
Thread: (current)
- Added OP_EXEC_STDIN opcode to bytecode.c and jacl.h
- Modified compiler__compile_pipe_op to detect AST_SHELL_CMD as pipe RHS:
  - When RHS is shell command, compile args vector first, then LHS (stdin source)
  - Emit OP_EXEC_STDIN instead of normal pipe rewriting
- Implemented OP_EXEC_STDIN in VM:
  - Pops stdin_val (string) and args_vec (vector)
  - Writes stdin string to temp file
  - Builds command with stdin redirect: `cmd args... < /tmp/stdin_file 2>/tmp/stderr_file`
  - Uses popen() to spawn process, returns stdout stream
  - Stores stdin temp file path in stream args[3] for cleanup
- Added stdin temp file cleanup in stream exhaustion (vm__pull_stream_one) and OP_COLLECT
- Updated test_bytecode.c: OP_EXEC_STDIN = 157, OP_HALT = 158
- Added 5 integration tests:
  - test_shell_stdin_string: basic string piped to cat
  - test_shell_stdin_multiline: multiline strings
  - test_shell_stdin_var: string variables piped to stdin
  - test_shell_stdin_grep: filtering stdin content
  - test_shell_stdin_expr: computed expression results piped to stdin
- Files changed: src/bytecode.c, src/compiler.c, src/jacl.h, src/vm.c, test/test_bytecode.c, test/test_integration.c
- **Learnings for future iterations:**
  - Lexer splits `-flag` into TOKEN_OPERATOR + TOKEN_WORD, causing flag args to be two separate args
  - Tests should avoid commands requiring dash-prefixed flags; use cat, grep with pattern, etc.
  - Pipe compilation order: args vector first, then stdin value, so OP_EXEC_STDIN pops in correct order
  - JACL `if` syntax requires blocks: `if $cond { then } else { else }`, not `[if $cond then else]`
  - Temp files need unique names using PID + time hash to avoid collisions across concurrent processes
---

## 2026-04-21 - US-008
Thread: (current)
- Extended OP_EXEC_STDIN to handle stream inputs in addition to strings
- When stdin_val is a stream, collect elements incrementally via vm__pull_stream_one():
  - Loop while src_stream->state != STREAM_EXHAUSTED
  - Handle error propagation from upstream (e.g., !false | !cat)
  - Convert elements to strings (string, i32, f64, repr for others)
  - Skip nil values
- Add trailing newlines for elements that don't end with \n:
  - Lines stream elements don't have trailing newlines
  - Exec stream elements (from !cmd) already have newlines
  - Auto-detect and add as needed
- Dynamic buffer allocation via arena (start 4096, grow 2x)
- Added 5 integration tests:
  - test_shell_stdin_stream_lines: [lines ...] | !cat
  - test_shell_stdin_stream_exec_chain: !echo | !cat (external to external)
  - test_shell_stdin_stream_grep: [lines ...] | !grep pattern
  - test_shell_stdin_stream_take: [lines ...] | take N | !cat
  - test_shell_stdin_stream_multi_chain: !cmd1 | !cmd2 | !cmd3
- Files changed: src/vm.c, test/test_integration.c
- **Learnings for future iterations:**
  - [lines ...] stream yields elements WITHOUT trailing newlines (stripped during parsing)
  - STREAM_KIND_EXEC yields elements WITH trailing newlines (from fgets)
  - When concatenating stream elements for stdin, add \n if element doesn't end with one
  - vm__pull_stream_one() handles all stream kinds uniformly (generator, filter, transform, etc.)
  - Stream collection for stdin materializes to memory - true lazy piping deferred to US-009 (OS pipes)
---

## 2026-04-21 - US-009
Thread: (current)
- Added OP_EXEC_PIPE opcode and STREAM_KIND_EXEC_PIPE constant
- Modified compiler to detect adjacent shell command chains:
  - compiler__is_shell_cmd_chain(): checks if node is shell cmd or pipe of shell cmds
  - compiler__count_shell_cmds(): counts commands in a chain
  - compiler__compile_shell_cmd_chain(): compiles all commands in order
  - compiler__compile_pipe_op(): detects `!cmd | !cmd` patterns, emits OP_EXEC_PIPE
- Implemented OP_EXEC_PIPE in VM using true Unix pipeline semantics:
  - Creates N pipes for N commands (last pipe captures final stdout)
  - Forks N processes, each with stdin/stdout connected via dup2()
  - Parent reads from last pipe's read end via fdopen()
  - Stores child PIDs in stream args for proper cleanup
- Implemented STREAM_KIND_EXEC_PIPE handling:
  - vm__pull_stream_one: reads lines from pipeline, waits for all children on EOF
  - OP_COLLECT: collects all output, waits for children, checks last exit code
  - Exit code of LAST command determines pipeline success (Unix semantics)
- Added 3 integration tests:
  - test_shell_os_pipe_grep: grep filtering through OS pipe
  - test_shell_mixed_pipes: shell -> JACL take -> shell
  - test_shell_pipe_last_exit: last command exit code determines result
- Files changed: src/bytecode.c, src/compiler.c, src/gc.c, src/jacl.h, src/vm.c, test/test_bytecode.c, test/test_integration.c
- **Learnings for future iterations:**
  - For N commands in pipeline, need N pipes (N-1 for connections + 1 for capturing last stdout)
  - Unix pipeline exit code is from LAST command, not first command that fails
  - Use fdopen() on pipe read end to get FILE* compatible with existing stream infrastructure
  - Store child PIDs in stream args (encoded as string) for waitpid() on stream exhaustion
  - fclose() doesn't waitpid(), must explicitly wait for all forked children
  - Mixed pipelines (shell -> JACL -> shell) don't use OP_EXEC_PIPE; they fall through to OP_EXEC_STDIN
---


## 2026-04-21 - US-010
Thread: (current)
- Added background execution support for shell commands (`!cmd &`)
- Parser changes:
  - Added `background` field to shell_cmd struct in AST
  - Parse TOKEN_AMP after shell command to set background flag
- Compiler changes:
  - Added OP_EXEC_BG opcode for background execution
  - AST_SHELL_CMD with background=1 emits OP_EXEC_BG instead of OP_EXEC
  - OP_AWAIT_SM now handles Job maps in addition to Futures
- VM implementation:
  - OP_EXEC_BG: fork(), redirect stdout/stderr to temp files, exec command
  - Returns Job map: {pid, _is_job, _stdout_path, _stderr_path}
  - OP_AWAIT_SM: detects Job maps via _is_job marker
  - Does blocking waitpid(), reads temp files, returns ExecResult {stdout, stderr, exit}
  - stdout is a STREAM_KIND_EXEC_BUFFER (same as [exec ...])
- Added 4 integration tests:
  - test_job_returns_map_with_pid: verify Job map structure
  - test_job_await_returns_result: verify await returns ExecResult
  - test_job_await_captures_stdout: verify stdout stream works
  - test_job_doesnt_block: verify parent continues immediately
- Files changed: src/ast.c, src/bytecode.c, src/compiler.c, src/jacl.h, src/parser.c, src/vm.c, test/test_bytecode.c, test/test_integration.c
- **Bug fix:** Tests originally used `def job = !cmd &` syntax, but JACL requires `job = !cmd &` (= operator expands to def). Fixed tests to use correct syntax.
- **Learnings for future iterations:**
  - JACL syntax: `x = value` expands to `[def x value]` via prelude macro
  - Writing `def x = value` parses as `[= [def x] value]` which gets wrong macro expansion
  - Use `job = !cmd &` not `def job = !cmd &`
  - Background jobs use temp files for stdout/stderr capture (not pipes)
  - waitpid() blocks until child exits; true non-blocking requires WNOHANG and polling
---

## 2026-04-21 - US-011
Thread: (current)
- Added signal/cancel support for background jobs
- Bytecode changes:
  - Added OP_SIGNAL opcode: pop signal_name + job, send signal, push $true/$false
  - Added OP_CANCEL opcode: pop job, send SIGTERM, push $true/$false
  - Added opcodes to both bytecode.c and jacl.h (dual definition pattern)
- Compiler changes:
  - Added "signal" and "cancel" to jacl_non_core_builtins
  - Added [signal $job SIGNAME] builtin: compile job + signal_name, emit OP_SIGNAL
  - Added [cancel $job] builtin: compile job, emit OP_CANCEL
- VM implementation:
  - OP_SIGNAL: validates job map (_is_job marker), extracts pid, parses signal name
  - Supports SIGTERM, SIGKILL, SIGINT, SIGHUP, SIGUSR1, SIGUSR2
  - Uses kill() syscall; returns $true on success, $false if ESRCH (process exited)
  - OP_CANCEL: same as OP_SIGNAL but hardcoded to SIGTERM
- Added 5 integration tests:
  - test_signal_job_sigterm: send SIGTERM to running job
  - test_signal_job_sigkill: send SIGKILL to running job
  - test_cancel_job: cancel running job
  - test_signal_completed_job: signal completed job returns $false
  - test_cancel_completed_job: cancel completed job returns $false
- Files changed: src/bytecode.c, src/compiler.c, src/jacl.h, src/vm.c, test/test_bytecode.c, test/test_integration.c
- **Learnings for future iterations:**
  - Numeric literals like `10` are parsed as integers - quote them for shell args: `!sleep "10"`
  - kill() returns -1 with errno=ESRCH if process doesn't exist (already exited)
  - signal.h provides SIGTERM, SIGKILL, etc. constants
  - OpCode enum is duplicated in both bytecode.c and jacl.h - must update both
---
