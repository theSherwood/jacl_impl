## M16 — GC/Concurrency Hardening Phase 3

Progress log for ralph/gc-concurrency-hardening-3 branch.

## Codebase Patterns
- Build and test with `bash build.sh` from project root. Individual test files compile via `gcc -Wall -Wextra -std=c99 -g test/test_gc.c -o /tmp/test_gc`
- JACL proc names must be <= 7 bytes (inline string limit). Use short names: `bump`, `trash`, `batch`, `loop`, `run`, etc.
- `while` loops with `set` don't work correctly inside spawned CPS closures (concurrent mode). Use tail recursion instead.
- .jacl integration tests use `# mode: concurrent` and `# expect:` headers. Test harness does exact string comparison on output.
- test_gc.c includes `../src/jacl.c` directly (unity build pattern) — no separate .o linking needed
- GCHeader is 8 bytes: uint32_t epoch + uint8_t mark + uint8_t obj_type + uint16_t alloc_total
- GC_BLOCK_SIZE = 65536 (64KB), GC_LINE_SIZE = 128, GC_LINES_PER_BLOCK = 512
- Tests use BlockPool + ThreadHeap directly; some tests use VM via `vm_init(&vm, &arena)`
- Pre-existing compiler warnings in hamt.h, rrb_vec.h, etc. are expected — only check for new warnings from changed files
- `gc__emergency_gc_fn` is `__thread` — clear it in tests using raw BlockPool/ThreadHeap to avoid stale callbacks from prior VM tests
- `gc_sweep_concurrent` watermark: use `UINT32_MAX` to sweep all objects; `0` makes everything immune (epoch >= 0 always true)
- gc_sweep_concurrent signature: (ThreadHeap*, GCBlock *skip, uint32_t watermark, uint8_t mark, BlockPool *pool). Pass pool=NULL in tests that don't need block recycling
- gc_sweep_concurrent is called from 3 places: gc_concurrent_collect (runtime.c), test_runtime.c, test_m12.c — all need updating when signature changes
- runtime__push_inbox and runtime__setup_call must be defined before all functions that call them (C declaration order in unity build)

---

## 2026-03-04 - US-001
- Eliminated deferred block recycling pattern: gc_sweep_concurrent now unlinks fully-empty blocks inline and returns them to the BlockPool directly
- Removed MAX_DEFERRED_BLOCKS constant, deferred arrays allocation in gc_concurrent_collect, and post-sweep unlinking loop
- New signature: gc_sweep_concurrent(heap, skip_block, watermark, current_mark, pool) — pool=NULL for test callers that don't need recycling
- Used pointer-to-pointer iteration pattern to safely unlink from singly-linked list while traversing
- Files changed: src/gc_collect.c, src/runtime.c, test/test_runtime.c, test/test_m12.c, test/test_gc.c
- **Learnings for future iterations:**
  - When modifying gc_sweep_concurrent, use `pool=NULL` in test callers that don't need actual block recycling (test_runtime.c, test_m12.c)
  - Pointer-to-pointer (`GCBlock **pp = &heap->blocks`) pattern is essential for safe inline unlinking during linked list traversal
  - test_gc_deferred_block_recycling test verifies blocks are returned to pool by checking heap block count decreased and pool free list grew
---

## 2026-03-04 - US-002
- Added detailed comment block above grey_buf_push in gc.c documenting the race scenario (worker realloc vs GC reader), why it's safe (release/acquire on count), and the critical invariant
- Added matching comment in gc__drain_grey_bufs in runtime.c referencing the acquire-on-count invariant
- Added test_gc_grey_buffer_drain_after_realloc in test_gc.c: pushes GREY_BUF_INIT_CAP * 3 real heap-allocated i64 objects, verifies cap grew, count matches, and all entries decode correctly
- Files changed: src/gc.c, src/runtime.c, test/test_gc.c
- **Learnings for future iterations:**
  - GreyBuffer is defined in gc.c (not a header), so test_gc.c can use it directly via unity build
  - grey_buf_init/push/destroy are static functions in gc.c — accessible from tests that include jacl.c
  - Use jacl_i64(&vm.heap, value) to create real heap-allocated objects for grey buffer tests (not sentinel values)
---

