## Codebase Patterns
- Single-header convention: `#ifndef X_H` / `#define X_H` for declarations, `#ifdef X_IMPLEMENTATION` for implementation
- Arena allocator: zero-init `arena_t arena = {0}`, uses libc by default
- Test tracked allocator: `tracked_allocator` from `test/test_helpers.h`, plug into arena via `.allocator = tracked_allocator`
- Build system: `build.sh` with `TESTS=()` array entries formatted as `"path/test.c|name|optional_extra_cflags"`
- Compiler flags: `-Wall -Wextra -std=c99 -g`
- Relative includes from module dirs use `../` prefix (e.g., `#include "../arena/arena.h"`)
- Implementation language is C (C99)
- Existing modules: platform/ (atomics, threading), arena/ (bulk allocation), test/ (test harness), lexer/ (tokenizer), value/ (tagged value system), parser/ (AST + parser)
- Private/internal functions use double-underscore prefix: `module__function_name`
- Public types use PascalCase (Token, LexResult, AstNode) or lower_t for system-level (arena_t)
- Public functions use module_verb_noun pattern (lexer_lex, arena_alloc, parser_parse)
- Constants and macros use ALL_CAPS (TOKEN_EOF, JACL_TAG_NIL)
- Test functions are `static int test_name(void)` returning 0=fail, 1=pass
- Test main() uses table-driven pattern: array of {name, fn} structs, iterates and counts pass/fail
- Memory leak check pattern: setup() -> exercise -> teardown() -> ASSERT(check_no_leaks()) -> TEST_PASS()
- Tests grouped by US-XXX story comments in the test table

- Lexer API: LexResult lexer_lex(const char* source, arena_t* arena)
- Token array always ends with TOKEN_EOF
- TOKEN_VAR payload.text points into source buffer, skips the $ prefix
- TOKEN_WORD payload.text points into source buffer
- TOKEN_STRING payload.text is arena-allocated decoded content
- TOKEN_OPERATOR handled same as TOKEN_WORD in parser

- Parser API: ParseResult parser_parse(LexResult tokens, arena_t* arena)
- ParseResult struct: AstNode** nodes, uint32_t count, uint32_t error_count
- AST node types: AST_COMMAND, AST_LIT_INT, AST_LIT_FLOAT, AST_LIT_STRING, AST_VAR_REF, AST_BLOCK, AST_INTERP_STRING, AST_ERROR
- AST_COMMAND has head (AstNode*) + args (AstNode**) + arg_count
- AstNode is a tagged union with SourcePos start/end

- Value API: JaclVal is a 64-bit tagged value in value/value.h
- Constructors: jacl_i32(val), jacl_f32(val), jacl_inline_string(ptr, len), JACL_NIL, JACL_TRUE, JACL_FALSE
- Arithmetic: jacl_add_i32, jacl_sub_i32, jacl_mul_i32, jacl_div_i32, jacl_mod_i32, jacl_neg_i32 (and _f32 variants)
- Comparison: jacl_lt_i32, jacl_gt_i32, jacl_le_i32, jacl_ge_i32 (and _f32 variants)
- Equality: jacl_eq (works across types)
- Type checks: jacl_is_i32, jacl_is_f32, jacl_is_nil, jacl_is_bool, jacl_is_inline_string, jacl_is_error
- Extraction: jacl_as_i32, jacl_as_f32, jacl_inline_string_ptr, jacl_inline_string_len

- Bytecode API: BytecodeChunk in compiler/bytecode.h, chunk_init, chunk_write, chunk_write_u16, chunk_add_constant
- OpCode enum: OP_CONST (+ u16 index), OP_NIL, OP_TRUE, OP_FALSE, OP_POP, OP_ADD..OP_MOD, OP_NEG, OP_EQ..OP_GE, OP_PRINT, OP_DEF_GLOBAL (+ u16), OP_GET_GLOBAL (+ u16), OP_HALT
- Bytecode arrays auto-grow via arena re-allocation (old memory left for arena to reclaim)
- Function declarations in header section must include `static` to match implementation section

- Compiler API: CompileResult compiler_compile(ParseResult, arena_t*) in compiler/compiler.h
- CompileResult struct: BytecodeChunk chunk, uint32_t error_count
- Compiler internal: Compiler struct with chunk*, arena*, error_count
- compiler__emit_byte, compiler__emit_u16, compiler__emit_constant helpers
- Bare top-level literals are wrapped by parser as AST_COMMAND{head: literal, args: [], arg_count: 0}
- Compiler handles AST_COMMAND with arg_count==0 by compiling just the head (bare expression)
- Test helper: compile_source(source, arena) chains lexer_lex -> parser_parse -> compiler_compile
- Compiler command dispatch: compiler__head_matches(head, name, len) checks if AST_LIT_STRING matches a builtin name
- compiler__compile_command dispatches known builtins (+, -, *, /, %) via if-chain with early returns
- compiler__compile_binary(c, args, opcode, line) compiles 2 args + emits opcode
- Unary negation: "-" with argc==1 emits OP_NEG; argc==2 emits OP_SUB
- VM__BINARY_NUMERIC_OP macro dispatches i32/f32 variants, returns VM_RUNTIME_ERROR on type mismatch
- OP_MOD is i32-only in VM; f32 operands produce VM_RUNTIME_ERROR
- Division by zero produces error-flagged value (VM_OK), not VM_RUNTIME_ERROR

- OP_PRINT: pops value, formats via print_fn, pushes JACL_NIL as return value
- Print formatting: error-flagged -> "<error>", nil -> "nil", bool -> "true"/"false", i32 -> %d, f32 -> %g, inline_string -> content
- Test print output: PrintCapture struct {buf[256], len} with capture_print callback set via vm.print_fn/vm.print_ctx

- VM API: VM struct in vm/vm.h, vm_init, vm_exec
- VMResult enum: VM_OK, VM_RUNTIME_ERROR, VM_STACK_OVERFLOW
- VM stack: JaclVal stack[256], stack_top is next free slot index
- VM has configurable print_fn + print_ctx, defaults to stdout via vm__default_print
- vm_exec takes VM* + BytecodeChunk*, returns VMResult
- Internal helpers: vm__push, vm__pop, vm__read_byte, vm__read_u16

---

## 2026-02-19 - US-001
- Implemented BytecodeChunk data structure and OpCode enum in compiler/bytecode.h
- Created compiler/test_bytecode.c with 13 tests covering all acceptance criteria
- Added test entry to build.sh TESTS array
- Files changed: compiler/bytecode.h (new), compiler/test_bytecode.c (new), build.sh (modified)
- **Learnings for future iterations:**
  - Header declarations must have `static` keyword to match the `static` definitions in the IMPLEMENTATION section
  - Arena-based growth: allocate new buffer, memcpy old data, point to new (old memory stays until arena_destroy)
  - BytecodeChunk tracks code_cap, const_cap, lines_cap separately for growth
---

## 2026-02-19 - US-002
- Implemented VM state struct, VMResult enum, vm_init, vm_exec dispatch loop in vm/vm.h
- Dispatch handles: OP_CONST, OP_NIL, OP_TRUE, OP_FALSE, OP_POP, OP_HALT
- Stack overflow/underflow detection with correct error codes
- Configurable print function (VMPrintFn callback + void* ctx)
- Created vm/test_vm.c with 14 tests covering all acceptance criteria
- Added test entry to build.sh TESTS array
- Files changed: vm/vm.h (new), vm/test_vm.c (new), build.sh (modified)
- **Learnings for future iterations:**
  - VM dispatch loop uses `for(;;)` with switch on instruction byte
  - vm__push returns VMResult so caller can propagate overflow immediately
  - vm__pop returns VMResult for underflow; takes JaclVal* out param
  - vm__read_u16 reads big-endian (hi byte first, matching chunk_write_u16)
  - Default case in switch returns VM_RUNTIME_ERROR for unknown/unimplemented opcodes
---

## 2026-02-19 - US-003
- Implemented compiler skeleton in compiler/compiler.h with single-header convention
- CompileResult struct, compiler_compile function, internal Compiler state
- Literal compilation: AST_LIT_INT -> OP_CONST(jacl_i32), AST_LIT_FLOAT -> OP_CONST(jacl_f32), AST_LIT_STRING (<=7) -> OP_CONST(jacl_inline_string)
- String >7 bytes produces compile error
- Multi-statement: OP_POP between statements, no pop after last
- OP_HALT emitted at end of every chunk
- AST_COMMAND with arg_count==0 handled as bare expression (compile head only)
- Created compiler/test_compiler.c with 14 tests
- Added test entry to build.sh TESTS array
- Files changed: compiler/compiler.h (new), compiler/test_compiler.c (new), build.sh (modified)
- **Learnings for future iterations:**
  - Parser wraps ALL top-level expressions in AST_COMMAND (even bare literals like `42`)
  - AST_COMMAND{head: literal, args: [], arg_count: 0} means "just evaluate the head"
  - `-7` is parsed as command `[- 7]` (operator + int), not as negative literal
  - Compile errors from parse errors are propagated by seeding result.error_count from parse.error_count
  - Test helper `compile_source(source, arena)` chains lex->parse->compile for convenience
---

## 2026-02-19 - US-004
- Implemented arithmetic builtins in compiler and VM
- Compiler: recognizes +, -, *, /, % command heads and emits OP_ADD, OP_SUB, OP_MUL, OP_DIV, OP_MOD, OP_NEG
- VM: dynamic type dispatch for i32/f32 on all arithmetic ops, OP_MOD i32-only, OP_NEG unary
- Division by zero produces error-flagged value (not runtime error)
- Added compiler__head_matches, compiler__compile_binary, compiler__compile_command helpers
- Added VM__BINARY_NUMERIC_OP macro for DRY binary op dispatch
- Files changed: compiler/compiler.h (modified), compiler/test_compiler.c (modified), vm/vm.h (modified), vm/test_vm.c (modified)
- Added 6 compiler tests + 13 VM tests, all 32 suite tests pass
- **Learnings for future iterations:**
  - Command head is AST_LIT_STRING (from TOKEN_OPERATOR in parser), compare with memcmp on value/length
  - compiler__compile_command is the central dispatch for builtin commands; extend with more if-chains for new builtins
  - VM__BINARY_NUMERIC_OP macro cleanly handles the pop-pop-typecheck-dispatch-push pattern
  - OP_MOD explicitly rejects f32 (not handled by the generic macro)
  - Forward declaration of compiler__compile_node needed since compiler__compile_binary calls it before definition
---

## 2026-02-19 - US-005
- Implemented comparison builtins (==, <, >, <=, >=) in compiler and VM
- Compiler: recognizes ==, <, >, <=, >= command heads and emits OP_EQ, OP_LT, OP_GT, OP_LE, OP_GE
- VM: OP_EQ uses jacl_eq (works across types, returns JACL_FALSE for different types)
- VM: OP_LT, OP_GT, OP_LE, OP_GE use VM__BINARY_NUMERIC_OP macro with i32/f32 dispatch
- Mismatched numeric types on ordered comparisons produce VM_RUNTIME_ERROR (same as arithmetic)
- Files changed: compiler/compiler.h (modified), compiler/test_compiler.c (modified), vm/vm.h (modified), vm/test_vm.c (modified)
- Added 3 compiler tests + 11 VM tests, all 32 suite tests pass (23 compiler, 38 VM)
- **Learnings for future iterations:**
  - OP_EQ is special: uses jacl_eq directly without type checking, so different types return false (not error)
  - Ordered comparisons (LT, GT, LE, GE) reuse VM__BINARY_NUMERIC_OP macro since they have same i32/f32 dispatch pattern
  - Multi-char operators like ==, <=, >= use compiler__head_matches with length 2
---

## 2026-02-19 - US-006
- Implemented print builtin in compiler and VM
- Compiler: recognizes "print" command head (5 chars), requires exactly 1 argument, emits OP_PRINT
- VM OP_PRINT: pops value, formats based on type (nil, true, false, i32, f32, inline_string, error), outputs via print_fn with trailing newline, pushes JACL_NIL as return value
- Print formatting: i32 uses %d, f32 uses %g, inline_string extracts content, error-flagged prints "<error>"
- Files changed: compiler/compiler.h (modified), compiler/test_compiler.c (modified), vm/vm.h (modified), vm/test_vm.c (modified)
- Added 3 compiler tests + 8 VM tests, all 26 compiler / 46 VM tests pass
- **Learnings for future iterations:**
  - OP_PRINT must push JACL_NIL as return value (print returns nil) to keep stack consistent
  - Use snprintf for numeric formatting into a stack buffer, then pass to print_fn
  - For inline strings, use jacl_inline_string_get to extract content, then manually append newline
  - Error-flagged values must be checked FIRST (before type checks) since error flag is orthogonal to type tag
  - PrintCapture test helper struct (buf + len) with capture_print callback is the pattern for testing print output
---
