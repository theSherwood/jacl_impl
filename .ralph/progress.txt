## Codebase Patterns
- Unity build: `src/jacl.c` #includes all pipeline .c files + lib headers as a single translation unit
- JACL pipeline files in `src/`: plain .c with `#ifndef X_C` guards, no `#define X_IMPLEMENTATION` needed
- Lib modules in `lib/`: single-header convention with `#ifdef X_IMPLEMENTATION`
- Test files: `#include "test_helpers.h"` then `#include "../src/jacl.c"` — gets everything
- Arena allocator: zero-init `arena_t arena = {0}`, uses libc by default
- Test tracked allocator: `tracked_allocator` from `test/test_helpers.h`, plug into arena via `.allocator = tracked_allocator`
- Build system: `build.sh` with `TESTS=()` array entries formatted as `"path/test.c|name|optional_extra_cflags"`
- Compiler flags: `-Wall -Wextra -std=c99 -g`
- Implementation language is C (C99)
- Pipeline: src/value.c → src/string.c → src/lexer.c → src/ast.c → src/parser.c → src/bytecode.c → src/compiler.c → src/vm.c
- Private/internal functions use double-underscore prefix: `module__function_name`
- Public types use PascalCase (Token, LexResult, AstNode) or lower_t for system-level (arena_t)
- Public functions use module_verb_noun pattern (lexer_lex, arena_alloc, parser_parse)
- Constants and macros use ALL_CAPS (TOKEN_EOF, JACL_TAG_NIL)
- Test functions are `static int test_name(void)` returning 0=fail, 1=pass
- Test main() uses table-driven pattern: array of {name, fn} structs, iterates and counts pass/fail
- Memory leak check pattern: setup() -> exercise -> teardown() -> ASSERT(check_no_leaks()) -> TEST_PASS()
- Tests grouped by US-XXX story comments in the test table

- Lexer API: LexResult lexer_lex(const char* source, arena_t* arena)
- Parser API: ParseResult parser_parse(LexResult tokens, arena_t* arena)
- Compiler API: CompileResult compiler_compile(ParseResult, arena_t*) in src/compiler.c
- VM API: VM struct in src/vm.c, vm_init(VM*, arena_t*), vm_exec(VM*, BytecodeChunk*)
- Pipeline API: jacl_run(source, vm, arena) in src/vm.c

- Value API: JaclVal is uint64_t with tag byte (bits 63-56) + 56-bit payload
- JACL_TAG_STRING (0x05) already reserved in value.c for heap string values
- JACL_TAG_INLINE_STRING (0x04) for strings up to 7 bytes stored in payload
- JACL_TAG_CLOSURE (0x08) for closure values
- Existing inline string helpers: jacl_inline_string(), jacl_inline_string_len(), jacl_inline_string_get(), jacl_is_inline_string()
- Existing generic pointer helpers: jacl_string_ptr() constructor, jacl_as_ptr() extractor
- Existing opcodes: OP_CONST, OP_NIL, OP_TRUE, OP_FALSE, OP_POP, OP_ADD..OP_MOD, OP_NEG, OP_EQ..OP_GE, OP_PRINT, OP_DEF_GLOBAL, OP_GET_GLOBAL, OP_GET_LOCAL, OP_SET_LOCAL, OP_GET_UPVALUE, OP_JUMP, OP_JUMP_IF_FALSE, OP_LOOP, OP_CALL, OP_RETURN, OP_CLOSURE, OP_POP_N, OP_HALT
- VM has single Environment (flat name/value arrays) for globals, pre-populated with true/false/nil
- Compiler dispatches builtins via compiler__head_matches if-chain in compiler__compile_command
- AST_BLOCK compilation: begin_scope, compile stmts with OP_POP after each, end_scope (OP_POP_N), push OP_NIL
- Compiler Local struct: { JaclVal name, int depth }, COMPILER_LOCALS_MAX = 256
- Compiler scope_depth: 0 = top-level (globals), >0 = inside block/function (locals)
- JaclClosure struct is in bytecode.c (not value.c) because it contains BytecodeChunk by value
- Typed closure helpers jacl_closure()/jacl_as_closure() are in bytecode.c; generic pointer helpers in value.c
- VM_FRAMES_MAX = 64; CallFrame struct: closure, return_ip, stack_base, chunk pointer
- vm_exec wraps top-level code in implicit JaclClosure (stack-allocated) for uniform frame-based execution
- OP_EQ dispatches to jacl_string_eq for strings, returns false for string-vs-non-string; OP_LT/GT/LE/GE dispatch to jacl_string_cmp for string-string
- VM has JaclInternTable* intern_table field for runtime string creation (concat, to_string); set in jacl_run()
- Adding new opcodes to the enum shifts OP_HALT — update test_bytecode.c opcode_enum test accordingly
- Compiler variadic builtin pattern: compile first 2 args + OP, then loop remaining args each with OP (pairwise chain)
- Truthiness: only nil and JACL_FALSE are falsy; 0, empty string, etc. are truthy
- In JACL, bare `false` is a string (truthy); `$false` is the boolean false (falsy)
- OP_TO_STRING: pops any value, pushes its string representation; no-op for strings; result inline <=7 or heap-interned
- Jump patching: compiler__emit_jump emits placeholder 0xFFFF, compiler__patch_jump fills in actual offset
- compiler__compile_block_expr: compiles block as expression (last stmt value stays on stack)
- Block commands inside { } must be separated by `;` or `\n`
- VM stack: JaclVal stack[256], stack_top is next free slot
- PrintCapture test pattern: struct {buf[1024], len} with capture_print callback
- Multiple top-level statements in jacl_run() MUST be separated by `\n`, not spaces — space-separated commands cause "cannot call nil value" runtime error

## M5-Specific Context
- Lexer already fully handles string interpolation: TOKEN_STRING, TOKEN_STRING_BEGIN/PART/END, TOKEN_INTERP_VAR, TOKEN_INTERP_EXPR_START/END
- Parser already builds AST_LIT_STRING and AST_INTERP_STRING nodes
- AST_LIT_STRING: { const char* value; uint32_t length; }
- AST_INTERP_STRING: { AstNode** segments; uint32_t count; } — segments are AST_LIT_STRING, AST_VAR_REF, or AST_COMMAND
- Compiler currently rejects strings > 7 bytes with error "string literal exceeds 7-byte inline limit"
- Compiler currently rejects AST_INTERP_STRING with error "interpolated strings not yet supported"
- No string comparison operators exist yet (== only works for numeric types)
- No string concatenation opcode or builtin exists
- Rope library (lib/sum_tree/rope.h) exists but is NOT used in M5 — deferred to later
- Key design: heap strings are NOT null-terminated, carry uint32_t length field
- String API: JaclHeapString struct in src/string.c: { uint32_t length, uint32_t hash, char data[] }
- Intern table: JaclInternTable struct, open addressing + linear probing, intern_table_init(), jacl_intern()
- jacl_is_heap_string() / jacl_as_heap_string() extractors in src/string.c
- FNV-1a hash: string__fnv1a(data, length) operates on byte data, length-aware (no null-terminator)
- Intern table resizes at > 0.75 load factor, initial cap 16
- Unified string API in src/string.c: jacl_string_len(), jacl_string_data(), jacl_string_eq(), jacl_string_cmp()
- jacl_is_string() in value.c returns true for BOTH inline and heap strings; use jacl_is_heap_string() for heap-only check
- jacl_string_data() copies into caller-provided buffer, returns actual length (may exceed buflen)
- jacl_string_cmp() uses direct pointer to heap data, stack buffer for inline data
- Compiler has JaclInternTable* intern_table field; compiler_compile() takes it as 3rd param
- jacl_run() creates a JaclInternTable on the stack and passes it to compiler_compile()
- Body compilers (proc) inherit the intern_table from the parent compiler
- compile_source() test helper in test_compiler.c creates its own JaclInternTable

---

## 2026-02-20 - US-001
- Implemented JaclHeapString struct with flexible array member (length, hash, data[])
- Implemented JaclInternTable with open addressing + linear probing, arena-allocated
- jacl_intern() returns JACL_TAG_STRING values with pointer equality for same byte sequences
- FNV-1a hash function operating on (data, length) pair
- Table resizes when load factor > 0.75
- Added jacl_is_heap_string() and jacl_as_heap_string() helpers
- Files changed: src/string.c (new), src/jacl.c (include string.c), test/test_string.c (new), build.sh (add test)
- **Learnings for future iterations:**
  - src/string.c is included between value.c and lexer.c in the pipeline
  - String intern table will need to be threaded through compiler and VM for later stories (US-003+)
  - Arena allocations don't need explicit cleanup, arena_destroy handles everything
---

## 2026-02-20 - US-002
- Implemented unified string API in src/string.c: jacl_string_len, jacl_string_data, jacl_string_eq, jacl_string_cmp
- Changed jacl_is_string() in value.c to return true for both JACL_TAG_INLINE_STRING and JACL_TAG_STRING
- Updated test_value.c NxN predicate matrix to use jacl_is_heap_string for the 1:1 type-tag mapping test
- Added 8 unit tests in test_string.c covering all representation combinations
- Files changed: src/string.c, src/value.c, test/test_string.c, test/test_value.c
- **Learnings for future iterations:**
  - jacl_is_string() now means "any string" (inline or heap); use jacl_is_heap_string() for heap-only
  - Cross-representation equality: if one is inline (<=7 bytes) and lengths match, both fit in 8-byte stack buffers
  - jacl_string_cmp avoids copying heap data by using direct pointer to JaclHeapString.data
  - When changing predicate semantics, check the NxN matrix tests in test_value.c
---

## 2026-02-20 - US-003
- Added JaclInternTable* to Compiler struct and compiler__init
- Changed compiler_compile() signature to accept JaclInternTable* as 3rd parameter
- AST_LIT_STRING with length > 7 now interns via jacl_intern instead of erroring
- Removed compile error "string literal exceeds 7-byte inline limit"
- Updated jacl_run() to create intern table and pass to compiler
- Body compilers (proc) inherit intern table from parent
- Updated existing tests that expected long string errors (test_compiler.c, test_integration.c)
- Added 5 unit tests in test_string.c: short still inline, long interns, dup share pointer, print/def compile
- Files changed: src/compiler.c, src/vm.c, test/test_compiler.c, test/test_integration.c, test/test_string.c
- **Learnings for future iterations:**
  - When changing error behavior (removing errors), search ALL test files for the old error message
  - Variable names still limited to 7 bytes (inline strings) — "greeting" is 8 chars, won't work as var name
  - compile_source() helper in test_compiler.c needs its own JaclInternTable
  - The intern table is stack-allocated in jacl_run() — it lives as long as the arena
---

## 2026-02-20 - US-004
- Extended OP_PRINT handler in vm.c to support heap strings via unified string API (jacl_string_len + jacl_string_data)
- Updated vm__type_name to use jacl_is_string() (recognizes both inline and heap) instead of just jacl_is_inline_string()
- Enlarged OP_PRINT stack buffer from 64 to 256 bytes, with overflow path for very long strings
- Added 5 end-to-end tests in test_string.c: heap string print, inline string print, long heap string, variable-stored heap string, mixed inline+heap sequence
- Files changed: src/vm.c, test/test_string.c
- **Learnings for future iterations:**
  - Multiple top-level JACL statements must be separated by `\n` (not just space) — space-separated commands fail with "cannot call nil value"
  - OP_PRINT now handles all string types uniformly; for strings > 255 bytes it falls through to direct print_fn calls
  - vm__type_name should use jacl_is_string() not jacl_is_inline_string() to cover both representations
---

## 2026-02-20 - US-005
- Modified OP_EQ in vm.c to dispatch to jacl_string_eq when either operand is a string; string vs non-string returns false (not error)
- Replaced VM__BINARY_NUMERIC_OP macro usage for OP_LT, OP_GT, OP_LE, OP_GE with expanded inline code that adds string comparison via jacl_string_cmp
- Added 9 end-to-end tests: inline-inline eq, heap-heap eq, different strings, string-vs-int mismatch, lt/gt ordering, heap string ordering, le/ge, and numeric-unchanged regression
- Files changed: src/vm.c, test/test_string.c
- **Learnings for future iterations:**
  - OP_LT/GT/LE/GE can't use VM__BINARY_NUMERIC_OP macro when adding string support — must expand inline to add the third type branch
  - For OP_EQ, string-vs-non-string should return false (not runtime error), matching dynamic language semantics
  - jacl_string_cmp returns int for ordering (negative/zero/positive), wraps memcmp + length comparison
  - Flag propagation is not needed for string comparison results since string JaclVals don't currently carry flags
---

## 2026-02-20 - US-006
- Added OP_CONCAT opcode to OpCode enum in src/bytecode.c
- Added JaclInternTable* intern_table field to VM struct for runtime string interning
- Implemented OP_CONCAT handler in vm.c: pops two strings, concatenates, result is inline if <=7 bytes or heap-interned otherwise
- Added `concat` builtin in compiler.c: variadic (2+ args), emits pairwise OP_CONCAT chain
- Set vm->intern_table in jacl_run() after creating the intern table
- Updated test_bytecode.c opcode enum assertions for OP_CONCAT (29) and OP_HALT (30)
- Added 8 tests in test_string.c: short+short inline, short+short heap result, short+long, long+long, variadic, empty+empty, interned result equality, variable concat
- Files changed: src/bytecode.c, src/compiler.c, src/vm.c, test/test_bytecode.c, test/test_string.c
- **Learnings for future iterations:**
  - Adding new opcodes shifts OP_HALT's enum value — always update test_bytecode.c opcode_enum test
  - VM needs JaclInternTable* for any runtime string creation (concat, to_string, etc.) — threaded via vm->intern_table set in jacl_run()
  - OP_CONCAT uses stack buffer (256 bytes) for concatenation; falls back to arena_alloc for very long strings
  - Compiler variadic builtin pattern: compile first 2 args + OP, then loop remaining args each with OP (pairwise chain)
- Compiler builtins with optional args: use OP_NIL as sentinel (e.g., slice 2-arg form pushes nil for end, VM checks for nil)
---

## 2026-02-20 - US-007
- Added OP_STR_LEN, OP_STR_INDEX, OP_STR_SLICE opcodes to bytecode.c
- Added `length`, `index`, `slice` compiler builtins in compiler.c
- Implemented VM handlers: OP_STR_LEN pops string pushes i32 length; OP_STR_INDEX pops i32+string, pushes single-char or nil; OP_STR_SLICE pops end+start+string, pushes substring
- Slice 2-arg form: compiler emits OP_NIL as end sentinel, VM detects nil and uses string length
- Slice bounds are clamped (no runtime error for out-of-range)
- Index out-of-bounds (negative or >= length) returns nil (not error)
- Result strings: inline if <= 7 bytes, heap-interned otherwise
- Updated test_bytecode.c: OP_STR_LEN=30, OP_STR_INDEX=31, OP_STR_SLICE=32, OP_HALT=33
- Added 16 tests in test_string.c covering all acceptance criteria
- Files changed: src/bytecode.c, src/compiler.c, src/vm.c, test/test_bytecode.c, test/test_string.c
- **Learnings for future iterations:**
  - Compiler builtins with optional args: emit OP_NIL as sentinel for missing args, VM checks with jacl_is_nil()
  - Slice bounds clamping avoids runtime errors — consistent with dynamic language semantics
  - OP_STR_INDEX uses direct data access (heap: hs->data[idx], inline: stack buffer) for efficiency
  - Adding 3 opcodes shifts OP_HALT by 3 — always update test_bytecode.c
- OP_TO_STRING uses snprintf for numeric types, memcpy for known short strings (nil/true/false)
- OP_TO_STRING for closures: uses cl->name if available ("<proc name>"), otherwise "<closure>"
---

## 2026-02-20 - US-008
- Added OP_TO_STRING opcode to OpCode enum in src/bytecode.c
- Implemented OP_TO_STRING handler in vm.c: pops value, converts to string representation, pushes result
- Conversions: i32→snprintf "%d", f32→snprintf "%g", bool→"true"/"false", nil→"nil", string→no-op, closure→"<proc name>" or "<closure>"
- Result is inline if <= 7 bytes, otherwise heap-interned via intern_table
- Updated test_bytecode.c: OP_TO_STRING=33, OP_HALT=34
- Added 10 tests in test_string.c: integer, negative int, float, true, false, nil, string no-op, closure, inline result check, heap result check
- Files changed: src/bytecode.c, src/vm.c, test/test_bytecode.c, test/test_string.c
- **Learnings for future iterations:**
  - OP_TO_STRING tests use direct bytecode construction (chunk_write) since there's no JACL syntax that emits OP_TO_STRING yet (US-009 will add it via interpolation)
  - When testing raw opcodes, create the VM manually with vm_init + set vm.intern_table before calling vm_exec
  - snprintf "%g" for floats avoids excessive trailing zeros (3.14f → "3.14", not "3.140000")
  - Adding 1 opcode shifts OP_HALT by 1 — always update test_bytecode.c
---

## 2026-02-20 - US-009
- Replaced AST_INTERP_STRING stub in compiler.c with real interpolation compilation
- Algorithm: compile first segment, then for each subsequent segment compile + OP_CONCAT
- AST_LIT_STRING segments compile as string constants (inline or heap-interned)
- AST_VAR_REF segments compile as variable lookup + OP_TO_STRING
- AST_COMMAND segments compile as command expression + OP_TO_STRING
- Empty interpolation (0 segments) emits empty string constant
- Adjacent interpolations handled naturally — no extra empty strings inserted
- Added 8 tests: variable interp, int variable coercion, command expr, multiple commands, adjacent vars, mixed var+command, only-var, heap result
- Files changed: src/compiler.c, test/test_string.c
- **Learnings for future iterations:**
  - String interpolation compilation is straightforward: segment-by-segment with pairwise OP_CONCAT (same pattern as variadic concat)
  - AST_LIT_STRING segments in interpolation don't need OP_TO_STRING (already strings); only AST_VAR_REF and AST_COMMAND do
  - Multiple top-level JACL statements in tests MUST be separated by `\n` — especially important for tests with [def ...] followed by [print ...]
  - No new opcodes needed — OP_TO_STRING and OP_CONCAT from US-008/US-006 provide all the infrastructure
---

## 2026-02-20 - US-010
- Created test/test_m5.c with 19 end-to-end integration tests using jacl_run() and PrintCapture pattern
- Tests cover: heap string print, concat with variables, length/index/slice builtins, string interpolation (var, expr, mixed), heap string equality, string ordering, heap strings as proc args/returns, closure capturing heap strings, variadic concat, slice one-arg form, string-vs-int eq, length on heap strings, index OOB returning nil, string while loop, to_string coercion via interpolation
- Added test_m5.c to build.sh TESTS array
- Updated DESIGN.md M5 row status to **COMPLETE**
- All 36 test suites pass (0 failures), zero memory leaks
- Files changed: test/test_m5.c (new), build.sh, DESIGN.md
- **Learnings for future iterations:**
  - Count string lengths carefully — "hello world this is long" is 24 chars, not 23
  - TEST_SETUP/TEST_TEARDOWN macros reduce boilerplate in integration test files
  - PrintCapture pattern: capture_print callback + {buf[1024], len} struct is the standard way to test print output
  - All M0-M4 tests continue to pass — no regressions from M5 string system
---
