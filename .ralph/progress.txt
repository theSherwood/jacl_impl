# Ralph Progress Log
Started: Tue Apr 29 2026
Feature: Implicit Context ($ctx) — Pool-allocated struct, explicit forking, per-field mutability

## Codebase Patterns
- StructTypeField is defined in BOTH `src/ast.c` (used by unity build) AND `src/jacl.h` (public API) — update both when changing struct type definitions
- StructTypeDef/StructTypeField/StructTypeRegistry are similarly duplicated between `src/compiler.c` and `src/jacl.h`
- Struct field mutation paths: inline (OP_STRUCT_SET_INLINE), heap (OP_STRUCT_SET), upvalue (OP_STRUCT_SET_UPVALUE), dynamic (OP_STRUCT_SET_DYN) — all four need updating for field-level checks
- Parser `mut` keyword token: TOKEN_MUT already existed in lexer, no new token needed
- Test patterns: `# expect-error:` for compile errors, `# expect:` for output validation in .jacl files
- C embedding API (jacl_struct_set_val in embed.c) bypasses compiler checks — operates at raw level
- Unity build order: value.c → gc.c → string.c → lexer.c → ast.c → parser.c → bytecode.c → collections.c → compiler.c → vm.c → syntax.c → gc_collect.c → runtime.c → embed.c
- TokenType enum is duplicated in BOTH `src/lexer.c` AND `src/jacl.h` — add new tokens to both
- New AST node types must also be handled in `src/syntax.c` (syntax_from_ast switch) to avoid -Wswitch warnings
- `jacl_run()` in vm.c now extracts actual parse error messages recursively from AST_ERROR nodes (not just "parse error")
- Parse error messages from jacl_run() are prefixed with "parse error: " for backwards compatibility
- ctx keyword: TOKEN_CTX in lexer, AST_CTX_DECL in parser, CtxFieldList collection in compiler
- Compiler struct is duplicated in `src/compiler.c` AND `src/jacl.h` — adding new fields requires updating BOTH (size mismatch causes stack corruption in tests)
- CtxFieldList is on root compiler, shared with module compilers via enclosing chain (like struct_registry)
- ctx struct is always registered via ctx_field_list__finalize after compilation; ctx_type_idx stored on StructTypeRegistry
- Tests that hard-code struct_registry->count need +1 when ctx struct is added
- JaclCtxPool is defined in vm.c (needs both gc.c and compiler.c types); also in jacl.h for tests
- VM struct is duplicated in `src/vm.c` AND `src/jacl.h` — adding new fields requires updating BOTH
- Pool free-list next pointer stored at byte 0 of JaclStruct data[] — overlaps field data, valid only when in pool
- GC root tracing for ctx pool: gc_mark (gc_collect.c) and gc_enumerate_roots (runtime.c) walk free-list from vm->ctx_pool
- vm->ctx is traced as GC root in both gc_mark (single-threaded) and gc_enumerate_roots (concurrent)
- ctx__init_vm helper in vm.c: initializes pool, allocates initial ctx from pool, sets pwd = getcwd(), sets vm->ctx
- Three execution entry points need ctx init: jacl_run, jacl_exec_program, jacl_ctx_run_source — all use stack-allocated JaclCtxPool
- jacl_rope_string_create creates GC-managed strings without needing an intern table (used for long pwd paths)
- jacl_inline_string handles strings ≤ 7 bytes; longer strings use jacl_rope_string_create

## 2026-04-29 - US-001
- Implemented per-field mutability on StructTypeField
- Added `is_mutable` flag to StructTypeField in both compiler.c and jacl.h
- Added `mut` keyword support in defstruct parser: `struct Foo {mut i32 x, i32 y}`
- Added `field_mutable` array to defstruct AST data (both ast.c and jacl.h)
- Compile-time mutability check in dot-set paths (both inline and heap)
- Runtime mutability check in OP_STRUCT_SET_DYN
- Files changed: src/ast.c, src/compiler.c, src/jacl.h, src/parser.c, src/vm.c, test/test_compiler.c, test/jacl/struct_set.jacl, test/jacl/struct_nested_set.jacl, test/jacl/struct_dyn.jacl
- New test files: test/jacl/struct_immutable_error.jacl, test/jacl/struct_mutable_field.jacl
- **Learnings for future iterations:**
  - The codebase has duplicated type definitions between .c files (for unity build) and .h files (for public API) — always check both
  - The `mut` keyword (TOKEN_MUT) was already defined in the lexer for variable declarations
  - Existing mutation tests needed `mut` annotation — 3 JACL files and 15 C test functions updated
  - Four separate opcode paths handle struct field writes — don't forget the dynamic path in vm.c
---

## 2026-04-29 - US-002
- Implemented ctx declaration — AST and parser
- Added TOKEN_CTX to TokenType enum in both lexer.c and jacl.h
- Added AST_CTX_DECL to AstNodeType enum in both ast.c and jacl.h
- Added ctx_decl data to AstNode union (is_mutable, type_name, field_name, default_expr)
- Implemented parser__parse_ctx_decl() with constant expression validation
- Wired TOKEN_CTX dispatch in parser_parse() top-level loop
- Added ctx-in-block error in parser__parse_cmd_operand()
- Added TOKEN_CTX to keyword fallback in parser__parse_atom()
- Added AST_CTX_DECL placeholder case in compiler.c
- Added AST_CTX_DECL case in syntax.c
- Fixed jacl_run() to extract actual parse error messages recursively
- Files changed: src/ast.c, src/compiler.c, src/jacl.h, src/lexer.c, src/parser.c, src/syntax.c, src/vm.c, test/test_parser.c
- New test files: test/jacl/ctx_decl_missing_default_error.jacl, test/jacl/ctx_decl_nested_error.jacl, test/jacl/ctx_decl_non_constant_error.jacl
- 10 new parser tests: basic, mutable, float/bool defaults, multiple, missing default, non-constant, nested block, nested bare block, struct constructor
- **Learnings for future iterations:**
  - TokenType is duplicated in lexer.c AND jacl.h — must add new tokens to both
  - New AST node types must have cases in syntax.c (syntax_from_ast) to avoid -Wswitch warnings
  - jacl_run() previously returned generic "parse error" — now extracts actual error message recursively
  - Parse error messages are prefixed "parse error: " for backwards compatibility with integration tests
  - The compiler has a no-op placeholder for AST_CTX_DECL — US-003 will add actual compilation
---

## 2026-04-29 - US-003
- Implemented ctx field collection and struct registration
- Added CtxFieldList type (CtxField struct + init/add/has/finalize helpers) in compiler.c
- Added ctx_type_idx field to StructTypeRegistry in both compiler.c and jacl.h
- Added ctx_fields pointer to Compiler struct in both compiler.c and jacl.h
- Pre-populated built-in `pwd` field (mut str) before user code compiles
- Implemented AST_CTX_DECL handler: resolves type, checks duplicates, adds to ctx field list
- Added ctx_field_list__finalize: builds StructTypeDef from accumulated fields, registers in StructTypeRegistry
- Shared ctx_fields from root compiler to module compilers (like struct_registry)
- Added finalization in both compiler_compile() and jacl_compile_program()
- Fixed 3 existing struct registry count assertions (now +1 for ctx struct)
- Files changed: src/compiler.c, src/jacl.h, test/test_compiler.c
- 7 new tests: ctx_builtin_pwd, ctx_single_user_field, ctx_multiple_fields, ctx_duplicate_field_error, ctx_duplicate_pwd_error, ctx_fields_across_modules, ctx_dup_across_modules_error
- **Learnings for future iterations:**
  - CRITICAL: Compiler struct is duplicated between compiler.c and jacl.h — adding new fields to one but not the other causes stack corruption in tests that allocate Compiler on the stack
  - ctx struct is always registered (even with no user ctx decls) because built-in pwd is always present
  - Tests that hard-code struct_registry->count values need updating when adding new always-registered types
  - Module compiler shares ctx_fields via root compiler (same pattern as struct_registry)
  - ctx_field_list__finalize uses forward-declared layout helpers (struct__type_size, etc.) — order matters in unity build
---

## 2026-04-29 - US-004
- Implemented ctx pool — GC-managed free-list of pre-allocated ctx structs
- Added JaclCtxPool struct with atomic free-list head, struct_size, type_idx, cached StructTypeDef pointer
- Implemented ctx_pool_init: pre-allocates 8 ctx structs via gc_alloc, links into atomic CAS free-list
- Implemented ctx_pool_alloc: atomic CAS pop from free-list; falls back to gc_alloc on empty pool
- Implemented ctx_pool_free: clears reference fields (set to JACL_NIL), then atomic CAS push to free-list
- Free-list next pointer stored at byte 0 of struct data[] (overlaps field data, valid only when in pool)
- Added ctx_pool field to VM struct (NULL until initialized)
- Added GC root tracing: gc_mark walks vm->ctx_pool free-list in gc_collect.c
- Added concurrent GC root tracing: gc_enumerate_roots walks worker vm.ctx_pool in runtime.c
- Files changed: src/vm.c, src/jacl.h, src/gc_collect.c, src/runtime.c, test/test_gc.c
- 5 new tests: ctx_pool_alloc_valid, ctx_pool_free_reuse, ctx_pool_exhaustion_fallback, ctx_pool_free_clears_refs, ctx_pool_gc_root
- **Learnings for future iterations:**
  - JaclCtxPool defined in vm.c (after compiler.c in unity build) because it needs both gc.c types (JaclStruct, gc_alloc) and compiler.c types (StructTypeDef)
  - VM struct is duplicated in vm.c AND jacl.h — must add new fields to BOTH (same as Compiler struct pattern)
  - Pool free-list uses atomic CAS with uintptr_t for lock-free thread safety (follows existing codebase patterns from gc.c/runtime.c)
  - gc__struct_registry is a file-static global in gc.c, but ctx pool is accessed via vm->ctx_pool (per-VM, not global)
  - When creating test struct registries, use calloc for reg.defs (not arena_alloc) since tests free it explicitly
---

## 2026-04-29 - US-005
- Implemented vm->ctx register and GC root integration
- Added JaclVal ctx field to VM struct in both vm.c and jacl.h
- Added vm->ctx initialization (JACL_NIL) in vm_init() and runtime__init_worker_vm()
- Added vm->ctx as GC root in gc_mark() (single-threaded, gc_collect.c)
- Added vm->ctx as GC root in gc_enumerate_roots() (concurrent, runtime.c — per worker)
- Created ctx__init_vm() helper: inits pool, allocs initial ctx from pool, sets pwd = getcwd()
- Integrated ctx__init_vm into three execution entry points: jacl_exec_program, jacl_run, jacl_ctx_run_source
- Files changed: src/vm.c, src/jacl.h, src/gc_collect.c, src/runtime.c, test/test_gc.c
- 3 new tests: vm_ctx_set_at_startup, vm_ctx_gc_survives, vm_ctx_multiple_gc_cycles
- **Learnings for future iterations:**
  - Three separate execution entry points (jacl_run, jacl_exec_program, jacl_ctx_run_source) all need ctx initialization — use the shared ctx__init_vm helper
  - JaclCtxPool is stack-allocated at each entry point, valid for the duration of execution (same pattern as intern_table in jacl_run)
  - jacl_rope_string_create works without an intern table — use it for runtime-generated strings like getcwd()
  - jacl_inline_string handles ≤7 byte strings; longer strings need jacl_rope_string_create
  - JACL_NIL is 0x00, so calloc-zeroed VMs already have ctx = NIL, but explicit init is clearer
  - gc__ms_push_val safely handles JACL_NIL (skips it), so workers with uninitialized ctx don't cause issues
---
