## Codebase Patterns
- Unity build: `src/jacl.c` #includes all pipeline .c files + lib headers as a single translation unit
- JACL pipeline files in `src/`: plain .c with `#ifndef X_C` guards, no `#define X_IMPLEMENTATION` needed
- Lib modules in `lib/`: single-header convention with `#ifdef X_IMPLEMENTATION`
- Test files: `#include "test_helpers.h"` then `#include "../src/jacl.c"` — gets everything
- Arena allocator: zero-init `arena_t arena = {0}`, uses libc by default
- Test tracked allocator: `tracked_allocator` from `test/test_helpers.h`, plug into arena via `.allocator = tracked_allocator`
- Build system: `build.sh` with `TESTS=()` array entries formatted as `"path/test.c|name|optional_extra_cflags"`
- Compiler flags: `-Wall -Wextra -std=c99 -g`
- Implementation language is C (C99)
- Pipeline: src/value.c → src/lexer.c → src/ast.c → src/parser.c → src/bytecode.c → src/compiler.c → src/vm.c
- Private/internal functions use double-underscore prefix: `module__function_name`
- Public types use PascalCase (Token, LexResult, AstNode) or lower_t for system-level (arena_t)
- Public functions use module_verb_noun pattern (lexer_lex, arena_alloc, parser_parse)
- Constants and macros use ALL_CAPS (TOKEN_EOF, JACL_TAG_NIL)
- Test functions are `static int test_name(void)` returning 0=fail, 1=pass
- Test main() uses table-driven pattern: array of {name, fn} structs, iterates and counts pass/fail
- Memory leak check pattern: setup() -> exercise -> teardown() -> ASSERT(check_no_leaks()) -> TEST_PASS()
- Tests grouped by US-XXX story comments in the test table

- Lexer API: LexResult lexer_lex(const char* source, arena_t* arena)
- Parser API: ParseResult parser_parse(LexResult tokens, arena_t* arena)
- Compiler API: CompileResult compiler_compile(ParseResult, arena_t*) in src/compiler.c
- VM API: VM struct in src/vm.c, vm_init(VM*, arena_t*), vm_exec(VM*, BytecodeChunk*)
- Pipeline API: jacl_run(source, vm, arena) in src/vm.c

- Value API: JaclVal is uint64_t with tag byte (bits 63-56) + 56-bit payload
- JACL_TAG_CLOSURE (0x08) already defined in value.c for closure values
- Existing opcodes: OP_CONST, OP_NIL, OP_TRUE, OP_FALSE, OP_POP, OP_ADD..OP_MOD, OP_NEG, OP_EQ..OP_GE, OP_PRINT, OP_DEF_GLOBAL, OP_GET_GLOBAL, OP_GET_LOCAL, OP_SET_LOCAL, OP_GET_UPVALUE, OP_JUMP, OP_JUMP_IF_FALSE, OP_LOOP, OP_CALL, OP_RETURN, OP_CLOSURE, OP_POP_N, OP_HALT
- VM has single Environment (flat name/value arrays) for globals, pre-populated with true/false/nil
- Compiler dispatches builtins via compiler__head_matches if-chain in compiler__compile_command
- AST_BLOCK compilation: begin_scope, compile stmts with OP_POP after each, end_scope (OP_POP_N), push OP_NIL
- Compiler Local struct: { JaclVal name, int depth }, COMPILER_LOCALS_MAX = 256
- Compiler scope_depth: 0 = top-level (globals), >0 = inside block/function (locals)
- def at scope_depth > 0: value stays on stack as local slot, push OP_NIL for def's return value
- $x resolution: check compiler__resolve_local first, fall back to OP_GET_GLOBAL
- Block commands inside { } must be separated by `;` or `\n` — parser treats same-line expressions as bare command args
- VM stack: JaclVal stack[256], stack_top is next free slot
- PrintCapture test pattern: struct {buf[1024], len} with capture_print callback
- JaclClosure struct is in bytecode.c (not value.c) because it contains BytecodeChunk by value
- Typed closure helpers jacl_closure()/jacl_as_closure() are in bytecode.c; generic pointer helpers in value.c
- VM_FRAMES_MAX = 64; CallFrame struct: closure, return_ip, stack_base, chunk pointer
- vm_exec wraps top-level code in implicit JaclClosure (stack-allocated) for uniform frame-based execution
- OP_CALL convention: closure at stack[stack_top - arg_count - 1], stack_base = stack_top - arg_count
- OP_RETURN: return value placed at callee_base - 1, stack_top reset to callee_base
- OP_RETURN from top-level (frame_count == 0): result in stack[0], stack_top = 1
- Truthiness: only nil and JACL_FALSE are falsy; 0, empty string, etc. are truthy
- OP_SET_LOCAL peeks (doesn't pop) — value stays on stack
- OP_JUMP_IF_FALSE pops the condition value from the stack
- vm->chunk is synced with current frame's chunk pointer throughout execution

---

## 2026-02-19 - US-001
- Added 10 new opcodes to OpCode enum in src/bytecode.c: OP_GET_LOCAL, OP_SET_LOCAL, OP_GET_UPVALUE, OP_JUMP, OP_JUMP_IF_FALSE, OP_LOOP, OP_CALL, OP_RETURN, OP_CLOSURE, OP_POP_N
- Updated test_bytecode.c: fixed hardcoded OP_HALT enum value, added new opcode assertions, added OP_RETURN to single-byte opcodes list
- Files changed: src/bytecode.c, test/test_bytecode.c
- **Learnings for future iterations:**
  - test_bytecode.c has hardcoded enum values in test_opcode_enum() — must update when adding opcodes
  - test_single_byte_opcodes list must include any new zero-operand opcodes
  - New opcodes placed before OP_HALT to keep it as the sentinel terminator
---

## 2026-02-19 - US-002
- Added JaclClosure struct to src/bytecode.c (after BytecodeChunk definition, since it contains a BytecodeChunk by value)
- Added typed helpers: jacl_closure(JaclClosure*) constructor and jacl_as_closure(JaclVal) extractor
- jacl_is_closure() and jacl_closure_ptr() already existed in value.c from earlier milestones
- Added 5 tests to test_bytecode.c: struct fields, constructor, extractor, full roundtrip, null name
- Files changed: src/bytecode.c, test/test_bytecode.c
- **Learnings for future iterations:**
  - JaclClosure struct must be in bytecode.c (not value.c) because it contains BytecodeChunk by value and value.c is included before bytecode.c in the unity build
  - The generic pointer helpers (jacl_closure_ptr, jacl_as_ptr, jacl_is_closure) are in value.c; the typed helpers (jacl_closure, jacl_as_closure) that need JaclClosure are in bytecode.c
  - All closure memory (struct, param_names, upvalues) should be arena-allocated via arena_alloc
---

## 2026-02-19 - US-003
- Added CallFrame struct and frame-based execution to src/vm.c
- VM_FRAMES_MAX = 64, frames[] array and frame_count added to VM struct
- vm_exec creates implicit top-level JaclClosure (stack-allocated) wrapping the input chunk
- Implemented 9 new opcode handlers: OP_GET_LOCAL, OP_SET_LOCAL, OP_GET_UPVALUE, OP_JUMP, OP_JUMP_IF_FALSE, OP_LOOP, OP_CALL, OP_RETURN, OP_POP_N
- Added vm__is_falsy() helper and updated vm__type_name() for closures
- Changed all chunk-> references in exec loop to vm->chunk-> for frame-aware access
- Added 15 tests to test/test_vm.c: frame_count_init, get/set_local, get_upvalue, jump, jump_if_false (falsy/truthy), loop, pop_n, basic_call_return, call_with_args, call_non_closure, call_arg_mismatch, call_frame_overflow, truthiness
- Files changed: src/vm.c, test/test_vm.c
- **Learnings for future iterations:**
  - OP_JUMP_IF_FALSE pops the condition — test assertions must account for one fewer stack item
  - Top-level implicit closure is stack-allocated (not arena) since it only lives for the vm_exec call
  - OP_CALL: closure sits at stack[stack_top - arg_count - 1]; params become locals 0..N-1 at stack_base
  - OP_RETURN from top-level frame: special case where frame_count == 0, put result in stack[0]
  - Self-referencing closures work for frame overflow tests: store closure in its own constant pool
  - vm->chunk must be kept in sync with the current frame's chunk throughout execution
---

## 2026-02-19 - US-004
- Added Local struct, COMPILER_LOCALS_MAX (256), scope_depth tracking to Compiler in src/compiler.c
- Added compiler__begin_scope/end_scope, compiler__add_local, compiler__resolve_local helpers
- Modified def handler: scope_depth > 0 → local (value stays on stack, push OP_NIL); scope_depth == 0 → global (OP_DEF_GLOBAL)
- Modified AST_VAR_REF: resolve_local first, fall back to OP_GET_GLOBAL
- Implemented AST_BLOCK compilation: begin_scope, compile stmts with OP_POP after each, end_scope (emits OP_POP_N for locals), push OP_NIL
- Added 9 tests to test/test_compiler.c: local_def_in_block, local_not_visible_outside, local_shadowing, local_nested_scopes, local_same_scope_shadow, local_top_level_still_global, local_top_level_var_ref_global, local_max_exceeded, local_empty_block
- Files changed: src/compiler.c, test/test_compiler.c
- **Learnings for future iterations:**
  - Commands inside blocks `{ }` must be separated by `;` or `\n` — the parser's bare command parser consumes consecutive expressions on the same line as args to a single command
  - Local slot index = position in the locals array; VM accesses as stack[frame.stack_base + slot]
  - Block compilation pops every statement result (including the last), then pushes nil — blocks don't return values yet (will be handled per-construct in if/while/proc stories)
  - end_scope walks locals array backwards, counts locals with depth > new scope_depth, emits single OP_POP_N
  - Shadowing in same scope creates new local slot (resolve_local searches backwards, finds latest)
---
