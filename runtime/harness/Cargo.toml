# Phase 1 runtime test harness. Compiles a C driver (which includes the JACL
# runtime) via temen-llvm and runs it on interp + JIT. `cargo test` here is the
# Phase 1 test runner. Own cargo root; path-deps into the svm submodule (links
# libLLVM — needs llvm-18-dev).
[workspace]

[package]
name = "jacl-runtime-harness"
version = "0.0.0"
edition = "2021"
publish = false

# `rlib` for the bins (jacl_svm, stage_macro, …); `staticlib` for `libjacl_runtime_harness.a`,
# which the native JACL frontend links to stage macros on SVM in-process (`jacl_svm_stage`,
# behind JACL_STAGE_ON_SVM — the Phase 5 embedding). See src/stage_ffi.rs.
[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
temen-ir     = { path = "../../vendor/svm/crates/temen-ir" }
temen-llvm   = { path = "../../vendor/svm/crates/temen-llvm" }
temen-text   = { path = "../../vendor/svm/crates/temen-text" }
temen-verify = { path = "../../vendor/svm/crates/temen-verify" }
temen-interp = { path = "../../vendor/svm/crates/temen-interp" }
temen-jit    = { path = "../../vendor/svm/crates/temen-jit" }
temen-run    = { path = "../../vendor/svm/crates/temen-run" }
temen-encode = { path = "../../vendor/svm/crates/temen-encode" }
temen-opt      = { path = "../../vendor/svm/crates/temen-opt" }
temen-wasm-jit = { path = "../../vendor/svm/crates/temen-wasm-jit" }
