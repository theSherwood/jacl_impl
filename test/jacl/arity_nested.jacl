# expect: 6
# expect: 20
# nested proc definitions with separate arities checked independently
proc outer [a, b] [
  proc inner [x, y, z] [ + [+ $x $y] $z ]
  inner $a $b 3
]
print [outer 1 2]
proc double [n] [ * $n 2 ]
print [double 10]
