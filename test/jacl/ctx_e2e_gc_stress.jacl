# expect: alive
# Test: GC stress — many ctx forks in a loop, strings stay alive
ctx mut str tag = "alive"

mut i 0
while [< $i 50] [
  with-ctx [tag "temp"] [
    def x $ctx->tag
  ]
  set i [+ $i 1]
]
print $ctx->tag
