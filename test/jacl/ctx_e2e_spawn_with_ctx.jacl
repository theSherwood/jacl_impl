# expect: 99
# expect: 42
# Test: spawn inside with-ctx inherits overridden ctx
ctx mut i32 level = 42

proc main [] [
  def f [with-ctx [level 99] [
    spawn [ $ctx->level ]
  ]]
  print [await $f]
  print $ctx->level
]
main
