# expect-error: generator tail expression produces a value
# try/catch recurses into body and handler; the body's tail produces
# a value that would be silently discarded.
proc gen [] [
  yield 1
  try [
    yield 2
    99
  ] e [
    yield 3
  ]
]
print [collect [gen]]
